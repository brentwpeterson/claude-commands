# Claude Code Template Repository - Exclude sensitive files

# NEVER commit local settings - contains sensitive permissions and paths
settings.local.json

# Exclude project-specific Claude.md files - keep generic template only
Claude.md
CLAUDE.md

# Exclude any real context files that might contain sensitive info
branch-context/*-context.md
!branch-context/feature-example-implementation-context.md
!branch-context/README.md

# CRITICAL: Exclude JSON memory files - may contain sensitive credentials
*.json
!settings.example.json
!settings.no-mcp.json
!package*.json

# Exclude real security scans
security-scans/*.md
!security-scans/README.md

# Exclude real violations
violations/*.md
!violations/README.md

# Exclude archived directories entirely - may contain sensitive historical data
archived-contexts/
archived-commands/

# Local workspace configuration - exclude actual config files but keep templates
local/workspace.env
local/project-overrides.json
local/user-preferences.json
local/*.local.*

# macOS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes

# Temporary files
*.tmp
*.temp
*~
!branch-context/master-context.md

# =============================================================================
# PROPRIETARY COMMANDS - Symlinked from .claude-local/commands/
# These are business-specific commands that should NEVER be in the public repo.
# The actual files live in .claude-local (separate git repo).
# =============================================================================

# Brand-specific commands
commands/brand-brent.md
commands/brand-cucumber.md
commands/brand-requestdesk.md

# Personal workflow commands
commands/brent-start.md
commands/brent-finish.md
commands/brent-task.md
commands/daily-content.md

# Business content commands
commands/add-backlog.md
commands/check-terms.md
commands/content-audit.md
commands/newsletter-planner.md
commands/cucumber-writer.md

# Tuesdays with Claude (personal brand)
commands/twc-backlog.md
commands/twc-finish.md
commands/twc-note.md
commands/twc-start.md
commands/twc-status.md

# Local files (gitignored, not symlinked)
local/
